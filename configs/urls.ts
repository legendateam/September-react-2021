import {IUrlsInterfaces} from "../interfaces/urls.interfaces";

const baseURL: string = 'https://jsonplaceholder.typicode.com';

export const urls:IUrlsInterfaces = {
    users: '/users',
    posts: '/posts'
}

export default baseURL